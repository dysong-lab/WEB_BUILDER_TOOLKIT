# Search

## MD3 정의

> Search app bars are used to input a search query.

## 역할

검색 입력 필드를 중심으로 구성된 앱바로, 사용자가 검색 쿼리를 입력할 수 있게 한다.

## 핵심 특성

- 검색 필드가 앱바 내에 직접 통합됨
- 검색 아이콘, 입력 필드, 선택적 후행 아이콘으로 구성
- 검색 활성화 시 검색 뷰로 확장 가능
- 검색 제안(suggestion) 목록을 표시할 수 있음
