# Menus

## MD3 정의

> Menus display a list of choices on a temporary surface.

## 역할

임시 표면에 선택 항목 목록을 표시한다. 화면에 상시 표시할 액션은 Toolbar를 사용한다.

## 핵심 특성

- 아이콘 버튼, 분할 버튼, 텍스트 필드 등 다양한 컴포넌트에서 열 수 있음
- 컨텍스트 메뉴는 이미지나 선택된 텍스트 등 특정 요소에 대한 액션을 제공하며, 보조 클릭으로 열림
