# Divider

## MD3 정의

> Dividers are thin lines that group content in lists or other containers.

## 역할

리스트나 컨테이너 내에서 콘텐츠를 그룹화하는 얇은 구분선이다.
데이터 바인딩 없이 순수 HTML + CSS로 구현한다.

## 핵심 특성

- 보이되 굵지 않게 표시
- 여백으로 그룹화할 수 없는 경우에만 사용
- 개별 항목 분리가 아닌 그룹 구분 목적으로 사용
