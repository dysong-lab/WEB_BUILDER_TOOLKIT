# NavigationRail

## MD3 정의

> Navigation rails let people switch between UI views on mid-sized devices.

## 역할

중형 디바이스에서 사용자가 UI 뷰 간을 전환할 수 있게 한다.

## 핵심 특성

- Medium, Expanded, Large, Extra-large 윈도우 크기에서 사용
- 3~7개 목적지 + 선택적 FAB 포함 가능
- 다른 화면에서도 항상 같은 위치에 배치
