# Filter

## MD3 정의

> Filter chips use tags or descriptive words to filter content.

## 역할

태그나 설명 키워드로 콘텐츠를 필터링하는 토글 선택 칩이다.

## 핵심 특성

- 선택/해제 토글 동작 (체크마크 아이콘으로 선택 상태 표시)
- 여러 개를 동시에 선택 가능
- 선택적 드롭다운 메뉴로 하위 필터 제공 가능
- 콘텐츠 영역 근처에 배치하여 필터링 맥락을 명확히 함
