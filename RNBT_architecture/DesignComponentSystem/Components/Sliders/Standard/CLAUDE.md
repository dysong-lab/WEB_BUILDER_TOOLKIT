# Standard

## MD3 정의

> Standard sliders allow users to make selections from a range of values.

## 역할

값 범위에서 단일 값을 선택할 수 있게 하는 기본 슬라이더이다.

## 핵심 특성

- 핸들 1개로 단일 값 선택
- 트랙 위를 드래그하거나 탭하여 값 설정
- 선택적 값 라벨(value indicator)이 핸들 위에 표시
- 연속(continuous) 또는 이산(discrete, tick mark) 모드 지원
