# Sliders

## MD3 정의

> Sliders let users make selections from a range of values.

## 역할

값 범위에서 사용자가 선택할 수 있게 한다.

## 하위 범주

| 하위 범주 | 역할 |
|----------|------|
| Basic | 단일 값 선택 (핸들 1개) |
| Centered | 중앙 기준점으로부터 오프셋 선택 (핸들 1개) |
| Range | 최솟값~최댓값 범위 선택 (핸들 2개) |
