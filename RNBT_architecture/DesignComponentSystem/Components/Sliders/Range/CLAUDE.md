# Range

## MD3 정의

> Range sliders allow users to select a range of values using two handles.

## 역할

두 개의 핸들로 최솟값과 최댓값 범위를 선택할 수 있게 하는 슬라이더이다.

## 핵심 특성

- 핸들 2개로 범위(min~max) 선택
- 두 핸들 사이의 활성 트랙으로 선택 범위를 시각적으로 표시
- 핸들이 서로 겹치지 않도록 최소 간격 유지
- 가격 범위, 시간 범위 등 범위 필터링에 적합
